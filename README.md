# dotnet-9-mvc

Just for fun and practice

Need to place ".env" file in the root of the repo with the below key-value pair for running the app. This is a Oracle DB connection string and can vary based on local or cloud-hosted db connection.

```
DEV_DB=USER ID=<user_id>;PASSWORD=<password>;DATA SOURCE=<data_source>;
```
